# my_keypass_manager
This is a simple keypass manager application.

## Preparation Steps
1. Create a virtual environment
python -m venv venv

2. Activate the virtual environment
venv\Scripts\activate

3. Install the required packages
pip install -r requirements.txt